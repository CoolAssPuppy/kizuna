# Kizuna CLI

Thin command-line wrapper around the Kizuna `/functions/v1/cli` endpoint.

```sh
npx kizuna login --url https://your-kizuna-host --token kzn_read_xxx
npx kizuna me itinerary --format md
npx kizuna logout
```
