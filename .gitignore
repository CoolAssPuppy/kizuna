# Claude Code and MCP configuration
.claude/settings.local.json
CLAUDE.local.md
mcp.json
.mcp.json

# Environment and secrets
.env
.env.local
.env.development
.env.test
.env.production
!.env.example

# Doppler. Local CLI state lives in ~/.doppler.
# Per-repo binding is committed via doppler.yaml (project scope only,
# no values), but any cached secret artifacts stay local.
.doppler/
doppler.local.yaml

# Dependencies and build artifacts
node_modules/
dist/
build/
.next/
coverage/
.turbo/

# Vite
*.local

# TypeScript incremental build artifacts
*.tsbuildinfo

# Vitest
.vitest/

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/

# IDE and editor files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo

# OS generated files
.DS_Store
Thumbs.db

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Supabase local
supabase/.branches
supabase/.temp
supabase/.env
supabase/seed_local.sql
tasks/community.md
